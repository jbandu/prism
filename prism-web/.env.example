# Database - Neon PostgreSQL (or local PostgreSQL)
# IMPORTANT: For Vercel deployment, add this to your Vercel project environment variables
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# Authentication
# Generate with: openssl rand -base64 32
# IMPORTANT: Use a strong secret for production deployments
NEXTAUTH_SECRET=your-secret-key-here

# NEXTAUTH_URL - Set to your deployment URL
# Local development:
NEXTAUTH_URL=http://localhost:3000
# Production (Vercel): Set this to your actual domain, e.g., https://your-app.vercel.app

# API Keys (optional - for future AI features)
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxx

# Map & Weather APIs (for Global Presence feature)
# Get your Mapbox token from: https://account.mapbox.com/access-tokens/
NEXT_PUBLIC_MAPBOX_TOKEN=pk.xxxxxxxxxxxxx

# Get your OpenWeather API key from: https://openweathermap.org/api
OPENWEATHER_API_KEY=xxxxxxxxxxxxx

# App Settings
NODE_ENV=development
